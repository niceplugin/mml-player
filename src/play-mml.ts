import { MML } from './index'
import { playSample } from './play-sample'
import { PlayNoteOptions } from './types'

export function playMml(this: MML, notes: PlayNoteOptions[]): void {
  // notes.forEach(note => {
  //   playSample.call(this, note, )
  // })
}